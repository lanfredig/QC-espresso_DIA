# <img src="QC-espresso.ico" width="40" height="40"> QC-espresso

# Proteomics Data QC – DIA
Quality Control Pipeline for DIA Proteomics  
**Documented Version:** v0.2.7  
**Author:** Guilherme Lanfredi

---

## How to Use (Step-by-Step)
This pipeline is designed for simple and direct use in laboratory routines, requiring no technical knowledge or command-line usage. To perform Quality Control (QC), follow these steps:

1. **Process your experiment** using DIA-NN software.
2. **Copy the file** `report.parquet` (resulting from DIA-NN processing) into the folder: `DIANN-out`.
3. **Copy the SAME FASTA file** used in DIA-NN processing to the pipeline folder, using the corresponding `FASTA` folder (when available).
4. **Double-click the shortcutbat file:** Proteomics_QC_launcher.bat` (this automatically triggers the QC pipeline).
5. **Wait for the automatic execution.**

### Output
Upon completion:
* An **HTML QC Report** is automatically generated.
* The HTML report is saved in the `Results` folder.
* A **CSV file** containing full protein coverage is also saved in the `Results` folder.
* The HTML report will open automatically in your default browser.

*No further interaction is required.*

---

## Development Status & Feedback
This launcher is under active development. The current version is fully functional and intended for routine QC reporting. Recent updates focused on improving robustness and determinism across different execution contexts, as well as providing a portable runtime for R and Quarto without altering the QC analytical behavior.

Suggestions and feedback are welcome, particularly regarding:
* Increased robustness across different Windows environments.
* Additional pre-execution validation checks.
* Clearer diagnostics for failure scenarios.
* Support for alternative execution modes.

---

## Requirements
The user only needs to provide:
* **`report.parquet`**: Output file generated by DIA-NN.
* **FASTA File**: The same database used during DIA-NN processing.

*All other components (scripts, launcher, runtime, and dependencies) are already included in the project.*

---

## Project Organization
The project is organized to separate **Usage** from **Implementation**. Users only interact with:
* `README.md`
* `run_QC_espresso` shortcut
* `DIANN-out` shortcut
* `Results` shortcut

All scripts, configurations, and runtimes are contained within the `Package` folder and do not need to be accessed manually.

---

## Key Feature: Portability (v0.2.7)
As of version v0.2.7, the pipeline focuses explicitly on **Portability**:
Use Control + Shift + m to toggle the tab key moving focus. Alternatively, use esc then tab to move to the next interactive element on the page.
Nenhum arquivo escolhido
Attach files by dragging & dropping, selecting or pasting them.
